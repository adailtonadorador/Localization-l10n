VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Maps Geocoding API (server-side, sem prefixo VITE_)
# Ative em: https://console.cloud.google.com > APIs & Services > Geocoding API
# Adicione no Vercel: Settings > Environment Variables
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Google Maps JavaScript API (client-side, prefixo VITE_ para expor ao browser)
# Ative "Maps JavaScript API" em: https://console.cloud.google.com > APIs & Services > Library
# Adicione no Vercel: Settings > Environment Variables
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Web Push Notifications (VAPID Keys)
# Gere as chaves em: https://vapidkeys.com/ ou use: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key

# Para a Edge Function (configurar no Supabase Dashboard > Edge Functions > Secrets)
# VAPID_PUBLIC_KEY=your_vapid_public_key
# VAPID_PRIVATE_KEY=your_vapid_private_key
# VAPID_EMAIL=mailto:contato@seudominio.com
